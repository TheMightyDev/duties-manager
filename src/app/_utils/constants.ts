/** The minimum amount of characters (excluding whitespace) the details of preference should contain */
export const MIN_PREFERENCE_DETAILS_LENGTH = 10;

export const DAYS_IN_MONTH = 30;

/** If a duty's score is equal to this constant, the granted score
 * for the duty is the organization's default for the
 * kind of duty.
 */
export const USE_DEFAULT_SCORE = -1;
